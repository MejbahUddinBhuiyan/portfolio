# Portfolio — Your Name

Simple static portfolio built with HTML and Tailwind CSS (CDN).

Open the site locally:

```powershell
# from repository root
Start-Process "index.html"
```

or open `index.html` in your browser. To customize:

- Edit `d:/New folder/Porfolio/index.html` and replace placeholders (name, email, project links).
- To use a local Tailwind build or add a bundler, initialize a Node project and install Tailwind.

Deploy options: GitHub Pages, Netlify, Vercel, or any static hosting.
